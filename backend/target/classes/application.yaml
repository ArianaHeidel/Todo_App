spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tododatabase
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: P0stgr3s123!

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

